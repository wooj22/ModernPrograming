//아래 문장에 대해 o x 로 표기하세요

생성자는 오버로드를 통해 여러 개 작성이 가능하다.              (  )

이미 생성된 객체에 대해 생성자는 명시적으로 호출할 수 있다.     (  )

소멸자는 유일하게 하나만 존재한다.                            (  )

소멸자가 선언되어 있으면 복사생성자는 자동으로 작성되지 않는다. (  )

이동생성자가 있으면 복사생성자는 자동으로 작성되지 않는다.      (  )

생성자는 가상 함수가 될 수 없다.                              (  )

소멸자는 가상 함수가 될 수 있다.                              (  )

부모 클래스의 friend 선언은 자식 클래스에서도 유효하다.        (  )
